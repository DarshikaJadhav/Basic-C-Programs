
        scanf(" %s", &d.drivingID);
        printf("Enter the Route: ");
        scanf(" %s", &d.route);
        printf("Enter the travelling distance in KM: ");
        scanf(" %d", &d.km);}

    for (int i = 0; i < n; i++)
    {
    
        printf("Name: %s\n", d.name);